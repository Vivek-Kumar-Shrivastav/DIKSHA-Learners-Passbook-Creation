export interface SubjectPerformanceCard {
    name: string;
    marksObtained: number;
    totalMarks: number;
    strength: string; // Assuming strength is a string, you can adjust the type as needed
}